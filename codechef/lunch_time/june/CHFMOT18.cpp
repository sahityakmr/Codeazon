#include "bits/stdc++.h"

#define int long long
#define endl "\n"
using namespace std;
bool dryRun = true;

int32_t main() {
    int n, testCount, arr[100];

    ifstream in(R"(..\codechef\res\input.txt)");
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    if (dryRun)
        cin.rdbuf(in.rdbuf());

    cin >> testCount;
    while (testCount--) {
        // Your code goes here.
    }
    return 0;
}